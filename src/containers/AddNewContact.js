import React, { PureComponent } from 'react';

class AddNewContact extends PureComponent {
  render() {
    return (
      <React.Fragment>
        <h1>This is where new contacts are added</h1>
      </React.Fragment>
    );
  }
}

export default AddNewContact;